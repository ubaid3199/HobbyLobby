version: "3.8"
services:
  db:
    environment:
      MYSQL_ROOT_PASSWORD: "yourpassword"
      MYSQL_DATABASE: "yourdatabase"
      MYSQL_USER: "root"
      MYSQL_PASSWORD: "yourpassword"
    ports:
      - "3308:3306"
